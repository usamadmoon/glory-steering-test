# Glory Steering Test
Read-only Android 9-compatible listener for `com.percherry.roundadas.LOOK_AROUND_360_CAN`.
The package name intentionally matches the target package used by the vehicle service so targeted broadcasts can be received.
